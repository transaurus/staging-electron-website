"use strict";(self.webpackChunkelectronjs=self.webpackChunkelectronjs||[]).push([["36196"],{97594:function(e,n,s){s.r(n),s.d(n,{frontMatter:()=>a,toc:()=>c,default:()=>h,metadata:()=>t,assets:()=>l,contentTitle:()=>i});var t=JSON.parse('{"id":"latest/tutorial/message-ports","title":"MessagePorts in Electron","description":"MessagePorts are a web feature that allow passing messages between different contexts. It\'s like window.postMessage, but on different channels. The goal of this document is to describe how Electron extends the Channel Messaging model, and to give some examples of how you might use MessagePorts in your app.","source":"@site/docs/latest/tutorial/message-ports.md","sourceDirName":"latest/tutorial","slug":"/latest/tutorial/message-ports","permalink":"/docs/latest/tutorial/message-ports","draft":false,"unlisted":false,"editUrl":"https://github.com/electron/electron/edit/main/docs/tutorial/message-ports.md","tags":[],"version":"current","frontMatter":{"title":"MessagePorts in Electron","description":"MessagePorts are a web feature that allow passing messages between different contexts. It\'s like window.postMessage, but on different channels. The goal of this document is to describe how Electron extends the Channel Messaging model, and to give some examples of how you might use MessagePorts in your app.","slug":"message-ports","hide_title":false},"sidebar":"docs","previous":{"title":"Process Sandboxing","permalink":"/docs/latest/tutorial/sandbox"},"next":{"title":"Performance","permalink":"/docs/latest/tutorial/performance"}}'),o=s(74848),r=s(84429);let a={title:"MessagePorts in Electron",description:"MessagePorts are a web feature that allow passing messages between different contexts. It's like window.postMessage, but on different channels. The goal of this document is to describe how Electron extends the Channel Messaging model, and to give some examples of how you might use MessagePorts in your app.",slug:"message-ports",hide_title:!1},i="MessagePorts in Electron",l={},c=[{value:"MessagePorts in the main process",id:"messageports-in-the-main-process",level:2},{value:"Extension: <code>close</code> event",id:"extension-close-event",level:2},{value:"Example use cases",id:"example-use-cases",level:2},{value:"Setting up a MessageChannel between two renderers",id:"setting-up-a-messagechannel-between-two-renderers",level:3},{value:"Worker process",id:"worker-process",level:3},{value:"Reply streams",id:"reply-streams",level:3},{value:"Communicating directly between the main process and the main world of a context-isolated page",id:"communicating-directly-between-the-main-process-and-the-main-world-of-a-context-isolated-page",level:3}];function d(e){let n={a:"a",code:"code",h1:"h1",h2:"h2",h3:"h3",header:"header",p:"p",pre:"pre",...(0,r.R)(),...e.components};return(0,o.jsxs)(o.Fragment,{children:[(0,o.jsx)(n.header,{children:(0,o.jsx)(n.h1,{id:"messageports-in-electron",children:"MessagePorts in Electron"})}),"\n",(0,o.jsxs)(n.p,{children:[(0,o.jsx)(n.a,{href:"https://developer.mozilla.org/en-US/docs/Web/API/MessagePort",children:(0,o.jsx)(n.code,{children:"MessagePort"})}),"s are a web feature that allow passing messages between\ndifferent contexts. It's like ",(0,o.jsx)(n.code,{children:"window.postMessage"}),", but on different channels.\nThe goal of this document is to describe how Electron extends the Channel\nMessaging model, and to give some examples of how you might use MessagePorts in\nyour app."]}),"\n",(0,o.jsx)(n.p,{children:"Here is a very brief example of what a MessagePort is and how it works:"}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='renderer.js (Renderer Process)'",children:"// MessagePorts are created in pairs. A connected pair of message ports is\n// called a channel.\nconst channel = new MessageChannel()\n\n// The only difference between port1 and port2 is in how you use them. Messages\n// sent to port1 will be received by port2 and vice-versa.\nconst port1 = channel.port1\nconst port2 = channel.port2\n\n// It's OK to send a message on the channel before the other end has registered\n// a listener. Messages will be queued until a listener is registered.\nport2.postMessage({ answer: 42 })\n\n// Here we send the other end of the channel, port1, to the main process. It's\n// also possible to send MessagePorts to other frames, or to Web Workers, etc.\nipcRenderer.postMessage('port', null, [port1])\n"})}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='main.js (Main Process)'",children:"// In the main process, we receive the port.\nipcMain.on('port', (event) => {\n  // When we receive a MessagePort in the main process, it becomes a\n  // MessagePortMain.\n  const port = event.ports[0]\n\n  // MessagePortMain uses the Node.js-style events API, rather than the\n  // web-style events API. So .on('message', ...) instead of .onmessage = ...\n  port.on('message', (event) => {\n    // data is { answer: 42 }\n    const data = event.data\n  })\n\n  // MessagePortMain queues messages until the .start() method has been called.\n  port.start()\n})\n"})}),"\n",(0,o.jsxs)(n.p,{children:["The ",(0,o.jsx)(n.a,{href:"https://developer.mozilla.org/en-US/docs/Web/API/Channel_Messaging_API",children:"Channel Messaging API"})," documentation is a great way to learn more about\nhow MessagePorts work."]}),"\n",(0,o.jsx)(n.h2,{id:"messageports-in-the-main-process",children:"MessagePorts in the main process"}),"\n",(0,o.jsxs)(n.p,{children:["In the renderer, the ",(0,o.jsx)(n.code,{children:"MessagePort"})," class behaves exactly as it does on the web.\nThe main process is not a web page, though\u2014it has no Blink integration\u2014and so\nit does not have the ",(0,o.jsx)(n.code,{children:"MessagePort"})," or ",(0,o.jsx)(n.code,{children:"MessageChannel"})," classes. In order to\nhandle and interact with MessagePorts in the main process, Electron adds two\nnew classes: ",(0,o.jsx)(n.a,{href:"/docs/latest/api/message-port-main",children:(0,o.jsx)(n.code,{children:"MessagePortMain"})})," and ",(0,o.jsx)(n.a,{href:"/docs/latest/api/message-channel-main",children:(0,o.jsx)(n.code,{children:"MessageChannelMain"})}),". These behave\nsimilarly to the analogous classes in the renderer."]}),"\n",(0,o.jsxs)(n.p,{children:[(0,o.jsx)(n.code,{children:"MessagePort"})," objects can be created in either the renderer or the main\nprocess, and passed back and forth using the ",(0,o.jsx)(n.a,{href:"/docs/latest/api/ipc-renderer#ipcrendererpostmessagechannel-message-transfer",children:(0,o.jsx)(n.code,{children:"ipcRenderer.postMessage"})})," and\n",(0,o.jsx)(n.a,{href:"/docs/latest/api/web-contents#contentspostmessagechannel-message-transfer",children:(0,o.jsx)(n.code,{children:"WebContents.postMessage"})})," methods. Note that the usual IPC methods like\n",(0,o.jsx)(n.code,{children:"send"})," and ",(0,o.jsx)(n.code,{children:"invoke"})," cannot be used to transfer ",(0,o.jsx)(n.code,{children:"MessagePort"}),"s, only the\n",(0,o.jsx)(n.code,{children:"postMessage"})," methods can transfer ",(0,o.jsx)(n.code,{children:"MessagePort"}),"s."]}),"\n",(0,o.jsxs)(n.p,{children:["By passing ",(0,o.jsx)(n.code,{children:"MessagePort"}),"s via the main process, you can connect two pages that\nmight not otherwise be able to communicate (e.g. due to same-origin\nrestrictions)."]}),"\n",(0,o.jsxs)(n.h2,{id:"extension-close-event",children:["Extension: ",(0,o.jsx)(n.code,{children:"close"})," event"]}),"\n",(0,o.jsxs)(n.p,{children:["Electron adds one feature to ",(0,o.jsx)(n.code,{children:"MessagePort"})," that isn't present on the web, in\norder to make MessagePorts more useful. That is the ",(0,o.jsx)(n.code,{children:"close"})," event, which is\nemitted when the other end of the channel is closed. Ports can also be\nimplicitly closed by being garbage-collected."]}),"\n",(0,o.jsxs)(n.p,{children:["In the renderer, you can listen for the ",(0,o.jsx)(n.code,{children:"close"})," event either by assigning to\n",(0,o.jsx)(n.code,{children:"port.onclose"})," or by calling ",(0,o.jsx)(n.code,{children:"port.addEventListener('close', ...)"}),". In the main\nprocess, you can listen for the ",(0,o.jsx)(n.code,{children:"close"})," event by calling ",(0,o.jsx)(n.code,{children:"port.on('close', ...)"}),"."]}),"\n",(0,o.jsx)(n.h2,{id:"example-use-cases",children:"Example use cases"}),"\n",(0,o.jsx)(n.h3,{id:"setting-up-a-messagechannel-between-two-renderers",children:"Setting up a MessageChannel between two renderers"}),"\n",(0,o.jsx)(n.p,{children:"In this example, the main process sets up a MessageChannel, then sends each port\nto a different renderer. This allows renderers to send messages to each other\nwithout needing to use the main process as an in-between."}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='main.js (Main Process)'",children:"const { BrowserWindow, app, MessageChannelMain } = require('electron')\n\napp.whenReady().then(async () => {\n  // create the windows.\n  const mainWindow = new BrowserWindow({\n    show: false,\n    webPreferences: {\n      contextIsolation: false,\n      preload: 'preloadMain.js'\n    }\n  })\n\n  const secondaryWindow = new BrowserWindow({\n    show: false,\n    webPreferences: {\n      contextIsolation: false,\n      preload: 'preloadSecondary.js'\n    }\n  })\n\n  // set up the channel.\n  const { port1, port2 } = new MessageChannelMain()\n\n  // once the webContents are ready, send a port to each webContents with postMessage.\n  mainWindow.once('ready-to-show', () => {\n    mainWindow.webContents.postMessage('port', null, [port1])\n  })\n\n  secondaryWindow.once('ready-to-show', () => {\n    secondaryWindow.webContents.postMessage('port', null, [port2])\n  })\n})\n"})}),"\n",(0,o.jsx)(n.p,{children:"Then, in your preload scripts you receive the port through IPC and set up the\nlisteners."}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='preloadMain.js and preloadSecondary.js (Preload scripts)' @ts-window-type={electronMessagePort:MessagePort}",children:"const { ipcRenderer } = require('electron')\n\nipcRenderer.on('port', e => {\n  // port received, make it globally available.\n  window.electronMessagePort = e.ports[0]\n\n  window.electronMessagePort.onmessage = messageEvent => {\n    // handle message\n  }\n})\n"})}),"\n",(0,o.jsxs)(n.p,{children:["In this example messagePort is bound to the ",(0,o.jsx)(n.code,{children:"window"})," object directly. It is better\nto use ",(0,o.jsx)(n.code,{children:"contextIsolation"})," and set up specific contextBridge calls for each of your\nexpected messages, but for the simplicity of this example we don't. You can find an\nexample of context isolation further down this page at ",(0,o.jsx)(n.a,{href:"#communicating-directly-between-the-main-process-and-the-main-world-of-a-context-isolated-page",children:"Communicating directly between the main process and the main world of a context-isolated page"})]}),"\n",(0,o.jsxs)(n.p,{children:["That means window.electronMessagePort is globally available and you can call\n",(0,o.jsx)(n.code,{children:"postMessage"})," on it from anywhere in your app to send a message to the other\nrenderer."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='renderer.js (Renderer Process)' @ts-window-type={electronMessagePort:MessagePort}",children:"// elsewhere in your code to send a message to the other renderers message handler\nwindow.electronMessagePort.postMessage('ping')\n"})}),"\n",(0,o.jsx)(n.h3,{id:"worker-process",children:"Worker process"}),"\n",(0,o.jsx)(n.p,{children:"In this example, your app has a worker process implemented as a hidden window.\nYou want the app page to be able to communicate directly with the worker\nprocess, without the performance overhead of relaying via the main process."}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='main.js (Main Process)'",children:"const { BrowserWindow, app, ipcMain, MessageChannelMain } = require('electron')\n\napp.whenReady().then(async () => {\n  // The worker process is a hidden BrowserWindow, so that it will have access\n  // to a full Blink context (including e.g. <canvas>, audio, fetch(), etc.)\n  const worker = new BrowserWindow({\n    show: false,\n    webPreferences: { nodeIntegration: true }\n  })\n  await worker.loadFile('worker.html')\n\n  // The main window will send work to the worker process and receive results\n  // over a MessagePort.\n  const mainWindow = new BrowserWindow({\n    webPreferences: { nodeIntegration: true }\n  })\n  mainWindow.loadFile('app.html')\n\n  // We can't use ipcMain.handle() here, because the reply needs to transfer a\n  // MessagePort.\n  // Listen for message sent from the top-level frame\n  mainWindow.webContents.mainFrame.ipc.on('request-worker-channel', (event) => {\n    // Create a new channel ...\n    const { port1, port2 } = new MessageChannelMain()\n    // ... send one end to the worker ...\n    worker.webContents.postMessage('new-client', null, [port1])\n    // ... and the other end to the main window.\n    event.senderFrame.postMessage('provide-worker-channel', null, [port2])\n    // Now the main window and the worker can communicate with each other\n    // without going through the main process!\n  })\n})\n"})}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-html",metastring:"title='worker.html'",children:"<script>\nconst { ipcRenderer } = require('electron')\n\nconst doWork = (input) => {\n  // Something cpu-intensive.\n  return input * 2\n}\n\n// We might get multiple clients, for instance if there are multiple windows,\n// or if the main window reloads.\nipcRenderer.on('new-client', (event) => {\n  const [ port ] = event.ports\n  port.onmessage = (event) => {\n    // The event data can be any serializable object (and the event could even\n    // carry other MessagePorts with it!)\n    const result = doWork(event.data)\n    port.postMessage(result)\n  }\n})\n<\/script>\n"})}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-html",metastring:"title='app.html'",children:"<script>\nconst { ipcRenderer } = require('electron')\n\n// We request that the main process sends us a channel we can use to\n// communicate with the worker.\nipcRenderer.send('request-worker-channel')\n\nipcRenderer.once('provide-worker-channel', (event) => {\n  // Once we receive the reply, we can take the port...\n  const [ port ] = event.ports\n  // ... register a handler to receive results ...\n  port.onmessage = (event) => {\n    console.log('received result:', event.data)\n  }\n  // ... and start sending it work!\n  port.postMessage(21)\n})\n<\/script>\n"})}),"\n",(0,o.jsx)(n.h3,{id:"reply-streams",children:"Reply streams"}),"\n",(0,o.jsxs)(n.p,{children:["Electron's built-in IPC methods only support two modes: fire-and-forget\n(e.g. ",(0,o.jsx)(n.code,{children:"send"}),"), or request-response (e.g. ",(0,o.jsx)(n.code,{children:"invoke"}),'). Using MessageChannels, you\ncan implement a "response stream", where a single request responds with a\nstream of data.']}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='renderer.js (Renderer Process)' @ts-expect-error=[18]",children:"const makeStreamingRequest = (element, callback) => {\n  // MessageChannels are lightweight--it's cheap to create a new one for each\n  // request.\n  const { port1, port2 } = new MessageChannel()\n\n  // We send one end of the port to the main process ...\n  ipcRenderer.postMessage(\n    'give-me-a-stream',\n    { element, count: 10 },\n    [port2]\n  )\n\n  // ... and we hang on to the other end. The main process will send messages\n  // to its end of the port, and close it when it's finished.\n  port1.onmessage = (event) => {\n    callback(event.data)\n  }\n  port1.onclose = () => {\n    console.log('stream ended')\n  }\n}\n\nmakeStreamingRequest(42, (data) => {\n  console.log('got response data:', data)\n})\n// We will see \"got response data: 42\" 10 times.\n"})}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='main.js (Main Process)'",children:"ipcMain.on('give-me-a-stream', (event, msg) => {\n  // The renderer has sent us a MessagePort that it wants us to send our\n  // response over.\n  const [replyPort] = event.ports\n\n  // Here we send the messages synchronously, but we could just as easily store\n  // the port somewhere and send messages asynchronously.\n  for (let i = 0; i < msg.count; i++) {\n    replyPort.postMessage(msg.element)\n  }\n\n  // We close the port when we're done to indicate to the other end that we\n  // won't be sending any more messages. This isn't strictly necessary--if we\n  // didn't explicitly close the port, it would eventually be garbage\n  // collected, which would also trigger the 'close' event in the renderer.\n  replyPort.close()\n})\n"})}),"\n",(0,o.jsx)(n.h3,{id:"communicating-directly-between-the-main-process-and-the-main-world-of-a-context-isolated-page",children:"Communicating directly between the main process and the main world of a context-isolated page"}),"\n",(0,o.jsxs)(n.p,{children:["When ",(0,o.jsx)(n.a,{href:"/docs/latest/tutorial/context-isolation",children:"context isolation"})," is enabled, IPC messages from the main process to\nthe renderer are delivered to the isolated world, rather than to the main\nworld. Sometimes you want to deliver messages to the main world directly,\nwithout having to step through the isolated world."]}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='main.js (Main Process)'",children:"const { BrowserWindow, app, MessageChannelMain } = require('electron')\n\nconst path = require('node:path')\n\napp.whenReady().then(async () => {\n  // Create a BrowserWindow with contextIsolation enabled.\n  const bw = new BrowserWindow({\n    webPreferences: {\n      contextIsolation: true,\n      preload: path.join(__dirname, 'preload.js')\n    }\n  })\n  bw.loadURL('index.html')\n\n  // We'll be sending one end of this channel to the main world of the\n  // context-isolated page.\n  const { port1, port2 } = new MessageChannelMain()\n\n  // It's OK to send a message on the channel before the other end has\n  // registered a listener. Messages will be queued until a listener is\n  // registered.\n  port2.postMessage({ test: 21 })\n\n  // We can also receive messages from the main world of the renderer.\n  port2.on('message', (event) => {\n    console.log('from renderer main world:', event.data)\n  })\n  port2.start()\n\n  // The preload script will receive this IPC message and transfer the port\n  // over to the main world.\n  bw.webContents.postMessage('main-world-port', null, [port1])\n})\n"})}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-js",metastring:"title='preload.js (Preload Script)'",children:"const { ipcRenderer } = require('electron')\n\n// We need to wait until the main world is ready to receive the message before\n// sending the port. We create this promise in the preload so it's guaranteed\n// to register the onload listener before the load event is fired.\nconst windowLoaded = new Promise(resolve => {\n  window.onload = resolve\n})\n\nipcRenderer.on('main-world-port', async (event) => {\n  await windowLoaded\n  // We use regular window.postMessage to transfer the port from the isolated\n  // world to the main world.\n  window.postMessage('main-world-port', '*', event.ports)\n})\n"})}),"\n",(0,o.jsx)(n.pre,{children:(0,o.jsx)(n.code,{className:"language-html",metastring:"title='index.html'",children:"<script>\nwindow.onmessage = (event) => {\n  // event.source === window means the message is coming from the preload\n  // script, as opposed to from an <iframe> or other source.\n  if (event.source === window && event.data === 'main-world-port') {\n    const [ port ] = event.ports\n    // Once we have the port, we can communicate directly with the main\n    // process.\n    port.onmessage = (event) => {\n      console.log('from main process:', event.data)\n      port.postMessage(event.data.test * 2)\n    }\n  }\n}\n<\/script>\n"})})]})}function h(e={}){let{wrapper:n}={...(0,r.R)(),...e.components};return n?(0,o.jsx)(n,{...e,children:(0,o.jsx)(d,{...e})}):d(e)}},84429:function(e,n,s){s.d(n,{R:()=>a,x:()=>i});var t=s(96540);let o={},r=t.createContext(o);function a(e){let n=t.useContext(r);return t.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function i(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(o):e.components||o:a(e.components),t.createElement(r.Provider,{value:n},e.children)}}}]);